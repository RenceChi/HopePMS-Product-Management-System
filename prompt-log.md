### 2 April 2026
#### Task:

Install Vitest testing framework.

#### Prompt:

"We are planning to implement Sprint 1. Can you walk us through on how to do it? No idea about git/github. Show us the step by step for each role"

""test": "vitest", "test:run": "vitest run" where in package.json i should put this"

#### What the AI Produced:

Let me build this as a proper HTML file you can download and open, so rendering issues won't be a problem.Here's your Sprint 1 guide as a downloadable HTML file. Download it and open it in any browser — it will work perfectly offline.

**What's inside:**
- Click each member tab (M1 through M5) on the left sidebar
- Each role has 6–7 detailed steps with exact code to copy-paste
- Click any step to expand it and read the instructions
- Tick "Mark as done" as you complete each step — the progress bar fills up
- Every step includes the exact terminal commands, SQL, and code needed

**Where to start on Day 1:**
- M1 → Step 1 (create accounts) and Step 2 (create the GitHub repo + invite everyone)
- M3 → Step 1 (create Supabase and share the URL/key with M1)
- Everyone else → Step 1 (clone the repo and set up their branch)

#### What you changed, rejected, or improved — and WHY:

I just did my dedicated task while I just showed the other members to help them as a guide.

#### What I learned as a result:

I learned how .json and .config files work more clearly.